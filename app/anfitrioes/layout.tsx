import DashboardTemplate from "../dashboard/DashboardTemplate";

export default function LayoutAnfitrioes({ children }: { children: React.ReactNode }) {
  return <DashboardTemplate>{children}</DashboardTemplate>;
}